const { default: stanfordColors } = require("./stanfordColors");

const barChartAxisTitle = {
  color: stanfordColors["brick"],
  font: {
    size: 22,
    weight: "bold",
  },
};

export { barChartAxisTitle };
