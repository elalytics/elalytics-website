const BookNameTag = ({ bookName }) => {
  return (
    <span className="px-4 py-1 bg-digital-blue-dark rounded text-white inline-block mb-1 text-sm font-bold">
      {bookName}
    </span>
  );
};

export default BookNameTag;
